<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Manage Addresses – Cake O'Clock</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Lato:wght@400;600;700&display=swap" rel="stylesheet">
  <%- include("../../views/partials/user/dashboardStyles") %>
</head>
<body>

<%- include("../../views/partials/user/h2") %>

<div class="page-wrap">

  <aside class="dash-sidebar">
    <%- include("../../views/partials/user/sider") %>
  </aside>

  <main class="dash-main" id="dashMain">

    <div class="page-topbar">
      <h2>My Addresses</h2>
      <a href="/add-address" class="btn-add spa-link">
        <i class="fa-solid fa-plus"></i> Add Address
      </a>
    </div>

    <div class="details-card">
      <div class="addr-list">
        <% if (!addresses || addresses.length === 0) { %>
          <div class="empty-addr">
<span class="icon"><i class="fa-solid fa-inbox" style="color:var(--rose)"></i></span>
           <h3>No addresses yet</h3>
            <p>Add your first delivery address to get started</p>
          </div>
        <% } else { %>
          <% addresses.forEach(address => {
const icons = {
  HOME: '<i class="fa-solid fa-house" style="color:var(--rose)"></i>',
  WORK: '<i class="fa-solid fa-briefcase" style="color:var(--rose)"></i>',
  FAMILY: '<i class="fa-solid fa-people-roof" style="color:var(--rose)"></i>',
  FRIEND: '<i class="fa-solid fa-handshake" style="color:var(--rose)"></i>'
};         %>
          <div class="addr-card" id="card-<%= address._id %>" style="position:relative;">
              <div class="addr-card-left">
<div class="addr-icon"><%- icons[address.type] || '<i class="fa-solid fa-location-dot" style="color:var(--rose)"></i>' %></div>
                <div>
                  <div class="addr-type-badge"><%= address.type %></div>
                  <div class="addr-name"><%= address.name %></div>
                  <div class="addr-phone"><%= address.phone %></div>
                  <div class="addr-line">
                    <%= address.street %>, <%= address.city %><br>
                    <%= address.state %> – <%= address.pincode %>
                  </div>
                </div>
              </div>
              <div class="menu-wrapper" style="position:absolute; top:16px; right:16px;">
                <div class="menu-icon" onclick="toggleMenu('<%= address._id %>')">
                  <i class="fa-solid fa-ellipsis-vertical"></i>
                </div>
                <div class="menu-dropdown" id="menu-<%= address._id %>">
                  <a href="/address/edit/<%= address._id %>" class="spa-link">
                    <i class="fa-solid fa-pen-to-square" style="color:var(--rose)"></i> Edit
                  </a>
                  <a href="javascript:void(0)" class="delete-link" onclick="openDeleteModal('<%= address._id %>')">
  <i class="fa-solid fa-trash" style="color:var(--rose)"></i> Delete
</a>
                </div>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>

    <!-- Delete Modal (kept inside #dashMain so it travels with AJAX swaps) -->
    <div id="deleteModal" class="modal-overlay">
      <div class="modal-box">
<span class="modal-icon"><i class="fa-solid fa-trash" style="color:var(--rose)"></i></span>  <h3>Delete Address?</h3>
        <p>This address will be permanently removed from your account.</p>
        <div class="modal-actions">
          <button class="btn-modal-cancel" onclick="closeDeleteModal()">Cancel</button>
          <button class="btn-modal-delete" onclick="confirmDelete()">Yes, Delete</button>
        </div>
      </div>
    </div>

    <script>
  let deleteAddressId = null;
  function openDeleteModal(id) {
    deleteAddressId = id;
    document.getElementById("deleteModal").style.display = "flex";
    document.querySelectorAll('.menu-dropdown').forEach(m => m.style.display = 'none');
  }
  function closeDeleteModal() {
    deleteAddressId = null;
    document.getElementById("deleteModal").style.display = "none";
  }
  function confirmDelete() {
    if (!deleteAddressId) return;
    const card = document.getElementById('card-' + deleteAddressId);
    if (card) {
      card.classList.add('removing');
      card.addEventListener('animationend', () => {
        window.location.href = `/address/delete/${deleteAddressId}`;
      }, { once: true });
    } else {
      window.location.href = `/address/delete/${deleteAddressId}`;
    }
  }
  function toggleMenu(id) {
    document.querySelectorAll('.menu-dropdown').forEach(m => {
      if (m.id !== 'menu-' + id) m.style.display = 'none';
    });
    const menu = document.getElementById('menu-' + id);
    const isOpen = menu.style.display === 'block';
    menu.style.display = isOpen ? 'none' : 'block';
    if (!isOpen) { menu.style.animation = 'none'; menu.offsetHeight; menu.style.animation = ''; }
  }
  document.addEventListener('click', function(e) {
    if (!e.target.closest('.menu-wrapper')) {
      document.querySelectorAll('.menu-dropdown').forEach(m => m.style.display = 'none');
    }
  });
    </script>

  </main>
</div>

</body>
</html>
