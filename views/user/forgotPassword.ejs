<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - Cake O Clock</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Outfit', sans-serif;
            background: linear-gradient(135deg, #fff0ed 0%, #ffe4e1 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .main-card {
            background: rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            width: 100%;
            max-width: 900px;
            border: 1px solid rgba(255, 255, 255, 0.6);
        }

        .form-section {
            padding: 4rem;
        }

        .image-section {
            background-color: #ffe4e1;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            position: relative;
        }

        .image-section img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .image-section:hover img {
            transform: scale(1.05);
        }

        h2 {
            color: #8c3a3a;
            /* Darker rosy color */
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        p.subtitle {
            color: #a67c7c;
            margin-bottom: 2rem;
            font-size: 0.95rem;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.5);
            border: 1px solid rgba(140, 58, 58, 0.2);
            padding: 0.8rem 1rem;
            border-radius: 10px;
            color: #5c2823;
        }

        .form-control:focus {
            background: #fff;
            box-shadow: 0 0 0 4px rgba(189, 123, 114, 0.2);
            border-color: #bd7b72;
        }

        .btn-primary {
            background-color: #8a3b3b;
            border: none;
            padding: 0.8rem 2rem;
            border-radius: 10px;
            font-weight: 600;
            width: 100%;
            margin-top: 1rem;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #6e2e2e;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(138, 59, 59, 0.3);
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 1.5rem;
            color: #a67c7c;
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.2s;
        }

        .back-link:hover {
            color: #8c3a3a;
        }

        /* Media Query for responsive image order */
        @media (max-width: 768px) {
            .form-section {
                padding: 2rem;
            }

            .image-section {
                height: 250px;
            }
        }
    </style>
</head>

<body>

    <div class="container p-3">
        <div class="row main-card m-0">
            <!-- Form Side -->
            <div class="col-md-6 form-section d-flex flex-column justify-content-center order-2 order-md-1">
                <div class="text-center mb-4">
                    <h2>Forgot Password?</h2>
                </div>

                <form action="/forgotPassword" method="POST">
                    <div class="mb-4">
                        <label for="emailInput" class="form-label visually-hidden">Email Address</label>
                        <p class="mb-2 text-start" style="color: #8c3a3a; font-weight: 500;">Please confirm your mail
                            address</p>
                        <input type="email" class="form-control" id="emailInput" name="email"
                            placeholder="Enter your email" required>
                    </div>

                    <button type="submit" class="btn btn-primary">Send Email</button>
                </form>

                <a href="/login" class="back-link">Go back to login page</a>
            </div>

            <!-- Image Side -->
            <div class="col-md-6 image-section p-0 order-1 order-md-2">
                <!-- IMPORTANT: User needs to ensure this path is correct or move the image -->
                <img src="C:/Users/hp/.gemini/antigravity/brain/f33e7ac5-5060-4213-87bd-b2a03351389d/forgot_password_cake_1769054053115.png"
                    alt="Delicious Cake">
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>