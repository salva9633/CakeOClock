<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create User Address</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    body { background:#dba39a; font-family:Outfit; }
    .dashboard-container { display:flex; width:98%; height:85vh; margin:20px auto; background:#fff; border-radius:30px; }
    .sidebar-area { width:260px; background:#fff5f2; padding:2rem; }
    .main-content { flex:1; padding:40px; overflow-y:auto; }
    .page-title { font-size:1.6rem; color:#AF5454; margin-bottom:30px; }
    .address-form { display:grid; grid-template-columns:1fr 1fr; gap:25px; max-width:900px; }
    .full-width { grid-column:1/-1; }
    .form-group { display:flex; flex-direction:column; gap:8px; }
    input { padding:12px; border:1px solid #ccc; border-radius:6px; }
    button { background:#d89696; border:none; padding:12px 30px; border-radius:6px; cursor:pointer; }
    .address-wrapper {
  display: flex;
  align-items: flex-start;
  gap: 30px;
}

.address-title {
  min-width: 120px; /* keeps label aligned */
}

.radio-column {
  display: flex;
  flex-direction: column;
  gap: 10px;
}



  </style>
</head>

<body>

<%- include("../../views/partials/user/h2") %>

<div class="dashboard-container">

  <aside class="sidebar-area">
    <%- include("../../views/partials/user/sider") %>
  </aside>

  <main class="main-content">
    <h1 class="page-title">Create User Address</h1>

<form action="/add-address" method="POST">

      <!-- Address Type -->
      <div class="form-group address-wrapper">
        <label class="address-title"><b>Address Type</b></label>
        <div class="radio-column">
          <label><input type="radio" name="type" value="HOME" checked> Home</label>
          <label><input type="radio" name="type" value="WORK"> Work</label>
          <label><input type="radio" name="type" value="OTHER"> Other</label>
        </div>

      </div>

      <div class="form-group">
        <label>Name</label>
        <input type="text" name="name" required>
      </div>

      <div class="form-group">
        <label>Pincode</label>
        <input type="text" name="pincode" required>
      </div>

      <div class="form-group">
        <label>Street</label>
        <input type="text" name="street" required>
      </div>

      <div class="form-group">
        <label>City</label>
        <input type="text" name="city" required>
      </div>

      <div class="form-group">
        <label>State</label>
        <input type="text" name="state" required>
      </div>

      <div class="form-group">
        <label>Phone</label>
        <input type="tel" name="phone" required>
      </div>

      <div class="full-width">
        <button type="submit">Save Address</button>
      </div>

    </form>
  </main>

</div>
</body>
</html>
