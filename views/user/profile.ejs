<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>

    <!-- Google Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        :root {
            --page-bg: #dba39a;
            --card-bg: #ffffff;
            --sidebar-bg: #fff5f2;
            --accent-brown: #5c2823;
            --label-brown: #8c5852;
            --field-bg: #fbdfdd;
            --btn-color: #bd7b72;
            --shadow-soft: 0 20px 40px rgba(189, 123, 114, 0.15);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Outfit', sans-serif;
        }

        body {
            background-color: var(--page-bg);
            height: 100vh;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* Stack header and content */
            align-items: center;
            justify-content: flex-start;
            /* Start from top */
            overflow: hidden;
            /* Unscrollable page */
        }

        /* Main Dashboard Card Container */
        .dashboard-container {
            display: flex;
            width: 100%;
            max-width: 98%;
            height: 90vh;
            /* Reduced height to fit header */
            margin-top: 20px;
            /* Space from header */
            background: var(--card-bg);
            border-radius: 30px;
            box-shadow: var(--shadow-soft);
            overflow: hidden;
            position: relative;
        }

        /* Sidebar Area (Left Panel) */
        .sidebar-area {
            width: 260px;
            background-color: var(--sidebar-bg);
            border-right: 1px solid rgba(0, 0, 0, 0.05);
            flex-shrink: 0;
            overflow-y: auto;
            overflow-x: hidden;
            padding: 2rem 1.5rem;
            /* Restored Padding */
        }

        /* Main Content Area (Right Panel) */
        /* Main Content Area (Right Panel) */
        .main-content {
            flex: 1;
            padding: 30px 40px;
            /* Adjusted padding */
            overflow-y: auto;
            /* Scrollable content area */
            display: flex;
            flex-direction: column;
            align-items: center;
            /* Centered content */
        }

        .profile-content-wrapper {
            width: 100%;
            max-width: 700px;
        }

        .profile-header {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 40px;
            text-align: center;
        }

        .avatar-circle {
            width: 110px;
            height: 110px;
            background-color: #f0cdc9;
            border-radius: 50%;
            overflow: hidden;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .profile-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .user-title {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--accent-brown);
        }

        .user-subtitle {
            font-size: 0.9rem;
            color: var(--label-brown);
        }

        .user-subtitle span {
            font-weight: 700;
            color: var(--accent-brown);
        }

        .section-header {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--accent-brown);
            margin-bottom: 25px;
            width: 100%;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
            margin-bottom: 30px;
            width: 100%;
        }

        @media (min-width: 768px) {
            .info-grid {
                grid-template-columns: 1fr 1fr;
            }
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .info-label {
            font-size: 0.75rem;
            text-transform: uppercase;
            color: var(--label-brown);
            font-weight: 700;
            margin-bottom: 8px;
        }

        .info-input {
            background-color: var(--field-bg);
            border-radius: 12px;
            padding: 16px;
            font-size: 1rem;
            color: var(--accent-brown);
            border: none;
        }

        .btn-edit {
            width: 100%;
            background-color: var(--btn-color);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 16px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            display: block;
            text-align: center;
            font-family: inherit;
        }

        .footer-text {
            margin-top: 20px;
            text-align: center;
            color: var(--label-brown);
            font-size: 0.85rem;
        }



        @media (max-width: 900px) {
            .dashboard-container {
                flex-direction: column;
                height: 95vh;
                margin-top: 20px;
            }

            .sidebar-area {
                width: 100%;
                height: auto;
                border-right: none;
                border-bottom: 1px solid rgba(0, 0, 0, 0.05);
                padding: 10px;
                /* If the included sidebar has specific styles, might need to ensure it flows horizontally or stacks */
                /* Assuming the included file has responsive classes or we control it here if possible */
            }

            .main-content {
                padding: 20px;
            }
        }
    </style>
</head>

<body>

    <%- include("../../views/partials/user/h2") %>

        <div class="dashboard-container">

            <!-- Sidebar Area -->
            <aside class="sidebar-area">
                <%- include("../../views/partials/user/sider") %>
            </aside>

            <!-- Main Content Area -->
            <main class="main-content">
                <div class="profile-content-wrapper">

                    <!-- PROFILE HEADER -->
                    <div class="profile-header">
                        <div class="avatar-circle">

                            <% if (user.authType==="google" && user.googleImage) { %>
                                <!-- Google profile image -->
                                <img src="<%= user.googleImage %>" class="profile-img" alt="Google Profile Image">

                                <% } else if (user.profileImage) { %>
                                    <!-- Cloudinary image -->
                                    <img src="<%= user.profileImage %>" class="profile-img" alt="Profile Image">

                                    <% } else { %>
                                        <!-- Default image -->
                                        <img src="/images/default-user.png" class="profile-img" alt="Default Image">
                                        <% } %>

                        </div>

                        <h1 class="user-title">
                            <%= user.name %>
                        </h1>

                        <p class="user-subtitle">
                            Joined On:
                            <span>
                                <%= new Date(user.createdAt).toDateString() %>
                            </span>
                        </p>
                    </div>

                    <!-- ACCOUNT INFO -->
                    <h2 class="section-header">User Profile</h2>

                    <div class="info-grid">

                        <div class="form-group">
                            <label class="info-label">Username</label>
                            <input class="info-input" value="<%= user.name %>" readonly>
                        </div>

                        <div class="form-group">
                            <label class="info-label">Gender</label>
                            <input class="info-input" value="<%= user.gender || 'N/A' %>" readonly>
                        </div>

                        <div class="form-group">
                            <label class="info-label">Email</label>
                            <input class="info-input" value="<%= user.email %>" readonly>
                        </div>

                        <div class="form-group">
                            <label class="info-label">Phone</label>
                            <input class="info-input" value="<%= user.phone || 'N/A' %>" readonly>
                        </div>

                    </div>

                    <a href="/editProfile" style="text-decoration: none;">
                        <button class="btn-edit">Edit Profile</button>
                    </a>

                    <% if (user.authType==="google" ) { %>
                        <p class="footer-text">
                            You signed in with Google. Email cannot be changed.
                        </p>
                        <% } %>

                </div>
            </main>

        </div>

</body>

</html>