<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - CakeOclock</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #FFF5F2;
            /* Soft Pink Background */
            --sidebar-width: 260px;
            --text-color: #4A3B32;
            /* Dark Brown */
            --accent-color: #C27ba0;
            /* Rose/Mauve */
            --hover-color: #F4D0CB;
            --active-bg: #D98880;
            --active-text: #FFFFFF;
            --card-bg: #FFFFFF;
            --btn-color: #AF5454;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Outfit', sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Sidebar Styling */
        .sidebar {
            width: var(--sidebar-width);
            background-color: var(--bg-color);
            padding: 2rem 1.5rem;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            transition: all 0.3s ease;
            z-index: 1000;
            border-right: 1px solid rgba(0, 0, 0, 0.05);
        }

        .brand {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 3rem;
            color: var(--btn-color);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .menu-item {
            display: flex;
            align-items: center;
            padding: 12px 16px;
            margin-bottom: 8px;
            border-radius: 12px;
            color: var(--text-color);
            text-decoration: none;
            transition: all 0.2s ease;
            font-weight: 500;
            cursor: pointer;
        }

        .menu-item i {
            width: 24px;
            margin-right: 12px;
            font-size: 1.1rem;
        }

        .menu-item:hover {
            background-color: var(--hover-color);
        }

        .menu-item.active {
            background-color: var(--btn-color);
            color: var(--active-text);
            box-shadow: 0 4px 15px rgba(175, 84, 84, 0.3);
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 2rem;
            transition: all 0.3s ease;
            width: calc(100% - var(--sidebar-width));
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        .page-title {
            font-size: 1.8rem;
            font-weight: 600;
            color: var(--btn-color);
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background-color: #ddd;
            border-radius: 50%;
            overflow: hidden;
        }

        .user-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Content Placeholder for Demo */
        .content-placeholder {
            min-height: 400px;
            border: 2px dashed #e0caca;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #a88d8d;
            font-size: 1.2rem;
            background: rgba(255, 255, 255, 0.5);
        }

        /* Dropdown Menu - Styled like reference image */
        .user-profile {
            position: relative;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .dropdown-menu {
            display: none;
            position: absolute;
            top: 130%;
            right: 0;
            background-color: #cb8d9e;
            /* Mauve/Dusty Pink from image */
            box-shadow: 0 10px 25px rgba(175, 84, 84, 0.2);
            border-radius: 12px;
            padding: 8px;
            min-width: 160px;
            z-index: 1002;
            border: none;
            /* No border for cleaner look */
            animation: fadeIn 0.2s ease-out;
        }

        /* Tiny triangle arrow at the top */
        .dropdown-menu::before {
            content: '';
            position: absolute;
            top: -6px;
            right: 20px;
            width: 12px;
            height: 12px;
            background-color: #cb8d9e;
            transform: rotate(45deg);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .dropdown-menu.show {
            display: block;
        }

        .dropdown-item {
            display: flex;
            align-items: center;
            padding: 10px 16px;
            color: #fff;
            /* White text for normal items */
            text-decoration: none;
            transition: all 0.2s;
            font-size: 0.9rem;
            border-radius: 8px;
            font-weight: 500;
        }

        .dropdown-item:hover {
            background-color: rgba(255, 255, 255, 0.15);
        }

        .dropdown-item i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
            color: rgba(255, 255, 255, 0.9);
        }

        /* Specific style for Logout to match the "red" text look in the image 
           (though on a dark pink background, dark red might have low contrast, 
           the image showed red text on a lighter pink. I will adjust background brightness).
        */
        /* Adjusting colors to match the image better: 
           Image description: Mauve background box. Red "Logout" text.
        */
        .dropdown-menu {
            background-color: #dcb6c2;
            /* Lighter mauve */
        }

        .dropdown-menu::before {
            background-color: #dcb6c2;
        }

        .dropdown-item {
            color: #7a4646;
            /* Darker brownish/red text */
        }

        .dropdown-item.logout {
            color: #b91c1c;
            /* Bright/Dark Red for logout */
            font-weight: 600;
            background-color: rgba(255, 255, 255, 0.3);
            /* Slight highlight box */
            margin-top: 5px;
        }

        .dropdown-item.logout i {
            color: #b91c1c;
        }

        /* Mobile Toggle */
        .mobile-toggle {
            display: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-color);
            z-index: 1001;
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.8);
            padding: 5px 10px;
            border-radius: 5px;
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .sidebar {
                transform: translateX(-100%);
                background: #FFF;
                box-shadow: 4px 0 15px rgba(0, 0, 0, 0.1);
            }

            .sidebar.active {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
                width: 100%;
                padding: 1.5rem;
            }

            .mobile-toggle {
                display: block;
            }

            /* Overlay when sidebar is open */
            .overlay {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: rgba(0, 0, 0, 0.3);
                z-index: 900;
            }

            .overlay.active {
                display: block;
            }
        }
    </style>
</head>

<body>

    <div class="mobile-toggle" onclick="toggleSidebar()">
        <i class="fas fa-bars"></i>
    </div>

    <div class="overlay" onclick="toggleSidebar()"></div>

    <aside class="sidebar">
        <div class="brand">
            cakeOclock
        </div>

        <nav id="sidebar-nav">

            <a href="/admin" class="menu-item">
                <i class="fas fa-th-large"></i>
                Dashboard
            </a>

            <a href="/admin/orders" class="menu-item">
                <i class="fas fa-shopping-bag"></i>
                Order
            </a>

            <a href="/admin/add-products" class="menu-item">
                <i class="fas fa-plus-square"></i>
                Add Products
            </a>

            <a href="/admin/products" class="menu-item">
                <i class="fas fa-box"></i>
                Products
            </a>

            <a href="/admin/sales-report" class="menu-item">
                <i class="fas fa-chart-line"></i>
                Sales report
            </a>


            <a href="/admin/users" class="menu-item">
                <i class="fas fa-users"></i>
                Customer
            </a>

            <a href="/admin/coupons" class="menu-item">
                <i class="fas fa-tag"></i>
                Coupon
            </a>

            <a href="/admin/Category" class="menu-item">
                <i class="fas fa-layer-group"></i>
                Category
            </a>

            <a href="/admin/brands" class="menu-item">
                <i class="fas fa-star"></i>
                Brands
            </a>

            <a href="/admin/banners" class="menu-item">
                <i class="fas fa-image"></i>
                Banner
            </a>

            <a href="/admin/returns" class="menu-item">
                <i class="fas fa-undo"></i>
                Refund/Return
            </a>

        </nav>
    </aside>

    <main class="main-content">
        <header class="header">
            <h1 class="page-title" id="page-title">Dashboard</h1>
            <div class="user-profile" onclick="toggleProfileDropdown()">
                <span>Admin</span>
                <div class="user-avatar">
                    <img src="https://ui-avatars.com/api/?name=Admin&background=AF5454&color=fff" alt="User">
                </div>

                <!-- Dropdown Menu -->
                <div class="dropdown-menu" id="profileDropdown">
                    <!-- Keeping just the logout prominent if that's what the user wants, 
                         but usually functionality requires others. 
                         I will make Logout the star based on the image style. -->
                    <a href="/admin/logout" class="dropdown-item logout">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </div>
            </div>
        </header>

        <!-- Content will follow here -->

        <script>
            // Toggle Sidebar for Mobile
            function toggleSidebar() {
                const sidebar = document.querySelector('.sidebar');
                const overlay = document.querySelector('.overlay');
                sidebar.classList.toggle('active');
                overlay.classList.toggle('active');
            }

            // Toggle Profile Dropdown
            function toggleProfileDropdown() {
                const dropdown = document.getElementById('profileDropdown');
                dropdown.classList.toggle('show');
            }

            // Close dropdown when clicking outside
            document.addEventListener('click', function (event) {
                const profile = document.querySelector('.user-profile');
                const dropdown = document.getElementById('profileDropdown');

                if (profile && !profile.contains(event.target)) {
                    if (dropdown && dropdown.classList.contains('show')) {
                        dropdown.classList.remove('show');
                    }
                }
            });

            // Highlight Active Tab based on URL
            document.addEventListener('DOMContentLoaded', function () {
                const currentPath = window.location.pathname;
                const menuItems = document.querySelectorAll('.menu-item');

                menuItems.forEach(item => {
                    const href = item.getAttribute('href');
                    // Exact match or partial match for sub-pages (excluding root admin path from partial matching to avoid all matches)
                    if (href === currentPath || (currentPath.startsWith(href) && href !== '/admin')) {
                        item.classList.add('active');

                        // Update Page Title
                        const pageTitle = document.getElementById('page-title');
                        if (pageTitle) {
                            pageTitle.innerText = item.innerText.trim();
                        }
                    } else if (currentPath === '/admin' && href === '/admin') {
                        item.classList.add('active');
                    } else {
                        item.classList.remove('active');
                    }
                });
            });
        </script>
</body>
</html>